// Chartist · Volume Profile — VPVR (mock).
import { VProfileView } from "@/components/chartist/vprofile/VProfileView";

export const revalidate = 60;

export default function ChartistVProfilePage() {
  return <VProfileView />;
}
