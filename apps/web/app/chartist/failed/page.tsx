// Chartist · Failed Breakouts — mock log of failed breakouts + lessons.
import { FailedView } from "@/components/chartist/failed/FailedView";

export const revalidate = 60;

export default function ChartistFailedPage() {
  return <FailedView />;
}
