// Chartist · Flow — 수급 대시보드 (mock).
import { FlowView } from "@/components/chartist/flow/FlowView";

export const revalidate = 60;

export default function ChartistFlowPage() {
  return <FlowView />;
}
