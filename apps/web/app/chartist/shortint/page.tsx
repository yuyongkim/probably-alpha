// Chartist · Short Interest — 공매도/대차 (mock).
import { ShortIntView } from "@/components/chartist/shortint/ShortIntView";

export const revalidate = 60;

export default function ChartistShortIntPage() {
  return <ShortIntView />;
}
