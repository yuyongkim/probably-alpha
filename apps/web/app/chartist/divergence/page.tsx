// Chartist · Divergence — RSI / MACD / OBV (mock).
import { DivergenceView } from "@/components/chartist/divergence/DivergenceView";

export const revalidate = 60;

export default function ChartistDivergencePage() {
  return <DivergenceView />;
}
