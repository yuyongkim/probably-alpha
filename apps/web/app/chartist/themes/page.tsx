// Chartist · Themes — 20 테마 로테이션 (mock).
import { ThemesView } from "@/components/chartist/themes/ThemesView";

export const revalidate = 60;

export default function ChartistThemesPage() {
  return <ThemesView />;
}
